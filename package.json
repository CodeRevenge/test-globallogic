{
  "name": "fee-calculator",
  "version": "1.0.0",
  "description": "A fee calculator.",
  "author": "Cesar A. Ojeda Escobar",
  "license": "ISC",
  "scripts": {
    "start": "node dist/main.js",
    "build:start": "npm run build && npm start",
    "start:dev": "nodemon src/main.ts",
    "build:start:no-color": "npm run build && NO_COLOR=true npm start",
    "start:dev:no-color": "NO_COLOR=true && nodemon src/main.ts",
    "build": "tsc -b",
    "test": "jest",
    "coverage": "jest --coverage",
    "lint": "eslint . --ext .ts --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@typescript-eslint/eslint-plugin": "^6.9.1",
    "@typescript-eslint/parser": "^6.9.1",
    "eslint": "^8.52.0",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "chalk": "^4.1.2"
  }
}
