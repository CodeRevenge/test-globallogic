import { Settings } from "./types/settings.type";

const settings: Settings = {
    currency: {
        style: 'currency',
        currency: 'USD',
        locale: 'en-US'
    }
};

export default settings;